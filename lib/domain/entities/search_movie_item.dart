class SearchMovieItem {
  final int? id;
  final String? posterPath;
  final String? title;
  final double? voteAverage;

  const SearchMovieItem({this.id, this.posterPath, this.title, this.voteAverage});
}
